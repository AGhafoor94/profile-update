<div class="box">
    <article class="media">
        <div class="media-left">
            <figure class="image is-64x64">
                <img src="{{owner.avatar_url}}" alt="Image">
            </figure>
        </div>
        <div class="media-content">
            <div class="content">
                <p>
                    <strong>{{name}}</strong>
                    <br>
                    <a href="{{clone_url}}">Link to the Github Repo</a></br>
                    {{description}}</br>
                    <a href="{{homepage}}">Homepage</a></br>
                    The language for this project is: {{language}}
                </p>
            </div>
        </div>
    </article>
</div>